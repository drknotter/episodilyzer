<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_margin="0dp"
    android:background="@android:color/white"
    android:orientation="vertical"
    android:padding="0dp" >

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" >

        <LinearLayout
            android:id="@+id/root_detail_container"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_margin="@dimen/show_detail_margin"
            android:orientation="vertical" >

            <include layout="@layout/show_title_container" />

            <TextView
                android:id="@+id/first_aired"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_gravity="center_horizontal"
                android:gravity="center"
                android:padding="@dimen/show_detail_first_aired_padding"
                android:textSize="@dimen/show_detail_first_aired_text_size" />
        </LinearLayout>
    </ScrollView>

    <Button
        android:id="@+id/add_show_button"
        android:layout_width="wrap_content"
        android:layout_height="@dimen/expandable_item_height"
        android:layout_gravity="center_horizontal"
        android:layout_margin="@dimen/show_detail_margin"
        android:gravity="center"
        android:text="@string/add_show_button_text" />

</LinearLayout>